# Providers

Purpose

Load external data.

Examples

FinancialDataProvider

RealtimeProvider

CSVProvider

FutureProvider

Rules

Provider only loads data.

Provider never analyzes.

Provider never scores.

Provider never decides.